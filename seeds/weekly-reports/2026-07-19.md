# Weekly report — 2026-07-19 (2026-W29)

## ⛔ Headline: Studio data collection blocked this run — 6th consecutive session, now with a W28 gap too

The camoufox-stealth MCP connector was unreachable this session (`camoufox-stealth_status` →
`Error | Not connected`, retried once per the CLAUDE.md transient-failure protocol, same result
both times). This is documented as unreachable across **6 consecutive routine sessions** in
`skills/voidline-manager/LEARNINGS.md` (community-manager RUN62/RUN63 on 07-18, this morning's
daily-short and daily-plan sessions on 07-19, and now this run) — a connector-level outage, not just
the separately-dead voidline cookie. As a result, Phases 1, 2, 4, and 6 of this routine (Studio
analytics, best/worst diagnostic, CTR/retention auto-actions, comment mining) have **no real data
this week**. No numbers below are estimated or fabricated — where data is missing, it says so.
Full detail: `skills/voidline-manager/LEARNINGS.md` `BLOCKER_2026-07-19-WEEKLY-INTEL`.

**New this week**: the 2026-W28 weekly-intel run (due 2026-07-12) appears to have **never executed
at all** — no snapshot/actions/feedback files exist for W28 on disk. See `self_eval/2026-W29.md`
for detail.

## Performance summary (last 30d)
Not available — Studio analytics unreachable (see headline). Known assets published in the last 30
days (IDs only, no metrics), compiled from `shorts/shorts_state.json`:
- Shorts: `w27_discovery_franklin` (06-30), `v4_hook` (07-01), `v4_answer` (07-01)
- Long-forms: `v4_roanoke` (07-01)

That is the **entire** last-30-day publish output — nothing has gone `PUBLIC` since 2026-07-01 (18
days). Full detail: `progress/snapshots/2026-W29.json`, which also lists 5 Shorts stuck
`PENDING_UPLOAD` (oldest 15 days overdue) and 2 long-forms (`LONG-1-hauser`, `LONG-2`/Cooper) that
were never produced or never re-scripted from stale prior-week content.

## Best / worst this week
Not available — requires the same Studio analytics as above, and there is no video published
strictly "this week" (W29, 07-13–07-19) to compare against — the closest candidate, the Sunday
discovery Short (`w29_discovery_nazca`), was produced end-to-end but is `PENDING_UPLOAD`.

## Experiments status
`experiment_tracker.py status`: 4 open, 0 closed — still over the 3-experiment cap first flagged
two weeks ago in `self_eval/2026-W27.md`, unresolved since. `experiment_tracker.py check`: all 4 now
past the 14-day age minimum (19d / 18d old) but none eligible — 3 of the 4 (EXP-HOOK-001,
EXP-TITLE-001, EXP-VOICE-001) are structurally stalled independent of the Studio outage, since their
required comparison videos were never actually published (only script/assets, blocked on the same
ElevenLabs/cookie chain). Detail: `self_eval/2026-W29.md`.

## CTR / retention diagnostics
Blocked — no data this week. `weekly_actions/2026-W29.md` explicitly marks this as unknown, not
"no issues found." No auto-actions (thumb re-render, voice change, duration cut, format pivot) were
queued.

## Trajectory
`progress/weekly_curve.csv` still has zero rows two runs after the channel's first weekly-intel run
(W27) — deliberately not appending a blank/zero row again this week, for the same reason as before:
it would corrupt the 4-week rolling average every future ETA calculation depends on.
`progress_curve.py eta` correctly reports "no data yet." First real row will land once Studio access
is restored and at least one week's `channel_aggregates` can be populated honestly.

## Viewer feedback
Blocked — Studio comments inbox unreachable, same outage. `viewer_feedback/2026-W29.json` is empty
with an explicit blocked-marker. **Idea Lock should fall back to the existing backlog**
(`weekly_plans/`, `skills/voidline-master/NEXT_VIDEOS.md`) for topic selection rather than reading
empty `requests: []` as "no requests this week" — consistent with the W27 precedent.

## Meta self-eval
Full detail in `self_eval/2026-W29.md`. Headline: the camoufox-stealth outage is now a multi-week,
6-consecutive-session recurrence, compounding with the pre-existing dead voidline cookie (17 days)
and exhausted ElevenLabs quota (resets 2026-07-30) — together these have stalled effectively the
whole production+publishing pipeline since 2026-07-02. Separately flagged: the still-unresolved
4-open-experiments cap breach, a new finding that 3 of those experiments are now bottlenecked on
production output (not just analytics access), and the newly-discovered 2026-W28 weekly-intel gap
(the routine never ran that week, rather than running and reporting blocked).

---

**Auto-actions queued for next week**: none — see `weekly_actions/2026-W29.md` (blocked, do not
treat as all-clear).
**Topic queue updated**: `viewer_feedback/2026-W29.json` is empty this week (blocked) — Idea Lock
should use the existing backlog instead.

**Owner action needed** (unchanged across 3 consecutive weekly-intel reports, all owner-side): (1)
restart/verify the camoufox-stealth MCP connector — unreachable 6 consecutive routine sessions; (2)
interactive voidline cookie re-login — dead 17 days since 2026-07-02; (3) ElevenLabs Creator quota
self-resets 2026-07-30. Fixing (1) and (2) unblocks Studio analytics, comment mining, and all
pending uploads in one action.
