# Weekly report — 2026-07-12 (2026-W28)

## ⛔ Headline: Studio data collection blocked this run (2nd consecutive week, different root cause)

The `voidline` cookie session is confirmed dead this session — reached
`studio.youtube.com`, landed on the Google account-chooser with Nolann marked
"Déconnecté," and `stealth_auth_check` returned `auth_valid=false / status=dead`.
Tried restoring from the most recent cookie backup (`voidline.bak1`) — no change.
This is **day 15** of the unrefreshed cookie (2026-07-02 → 2026-07-12), the same
signature logged continuously since RUN19 (2026-07-05). Unlike last week's W27
blocker (the camoufox-stealth/mcphub bridge itself was unreachable), the bridge works
fine this week — the session auth is what's invalid. Net effect is identical: Phases
1/2/4/6 of this routine (Studio analytics, best/worst diagnostic, CTR/retention
auto-actions, comment mining) have **no real data this week**. Full detail:
`skills/voidline-manager/LEARNINGS.md` `BLOCKER_2026-07-12-WEEKLY-INTEL`,
`progress/snapshots/2026-W28.json`.

## Performance summary (last 30d)
Not available — Studio analytics unreachable (see headline). No new video has actually
published on the channel since 2026-07-01 anyway — every Short since 2026-07-04 and
both W28 long-forms are stuck `PENDING_UPLOAD`/unrendered on the same dead cookie (plus
an independent ElevenLabs quota exhaustion for the long-forms, resets 2026-07-30). Known
published assets in the last 30 days (IDs only, no metrics), compiled from
`shorts/shorts_state.json`: Shorts — v3_answer (06-12), v1_bonus_briggs (06-15),
w27_discovery_franklin (06-30), v4_hook (07-01), v4_answer (07-01). Long-forms —
v4_roanoke (07-01). Full detail: `progress/snapshots/2026-W28.json`.

## Best / worst this week
Not available — requires the same Studio analytics as above.

## Experiments status
4 open, 0 closed (`experiment_tracker.py status`): EXP-HOOK-001, EXP-TITLE-001,
EXP-VOICE-001 (since 2026-06-30, 12d old), PASSIVE-TIME-001 (since 2026-07-01, 11d old).
None eligible for a verdict yet (need 14d — `experiment_tracker.py check` confirms all
"too young"). **Still 4 open against the 3-experiment cap**, unresolved from W27 — do
not open a 5th until one closes (earliest 2026-07-14). Detail: `self_eval/2026-W28.md`.

## CTR / retention diagnostics
Blocked — no data this week. `weekly_actions/2026-W28.md` explicitly marks this so
Production routines treat it as "unknown," not "no issues found." No auto-actions
(thumb re-render, voice change, duration cut, format pivot) were queued. Carried
forward: the 15-day-old cookie blocker, the ElevenLabs quota block, and the
experiment-cap overshoot are all listed there as standing owner/maintenance items.

## Trajectory
`progress_curve.py eta` still reports "no data yet" — `progress/weekly_curve.csv` has
no rows. Deliberately not appending a blank/zero row this week either (2nd week in a
row): an all-unknown data point this early would corrupt the 4-week rolling average
every future ETA calculation depends on. First real row lands once Studio access is
restored and a real weekly-intel run collects `channel_aggregates`.

## Viewer feedback
Blocked — Studio comments inbox unreachable, same dead-cookie root cause.
`viewer_feedback/2026-W28.json` is empty with an explicit blocked-marker. **Idea Lock
at 14:00 should fall back to the existing backlog** (`weekly_plans/`,
`monthly_intel/2026-07-actions.json`, `skills/voidline-master/NEXT_VIDEOS.md`) for topic
selection rather than reading empty `requests: []` as "no requests this week" — same
guidance as W27.

## Meta self-eval
Full detail in `self_eval/2026-W28.md`. Headline: this is the 2nd consecutive
weekly-intel run blocked on Studio access, but the root cause changed from an
infra outage (W27) to a 15-day-old dead auth session (W28) that has not self-resolved
across dozens of independent routine-session checks and is gated on a single
interactive owner action (re-login to the `voidline` cookie profile) no routine can
perform. Also re-flagged: the 4-open-experiments cap breach, still unresolved from W27.

---

**Auto-actions queued for next week** : none — see `weekly_actions/2026-W28.md`
(blocked, do not treat as all-clear).
**Topic queue updated** : `viewer_feedback/2026-W28.json` is empty this week (blocked)
— Idea Lock should use the existing backlog instead.
**Owner action needed (unchanged, now 15 days outstanding)**: interactive re-login to
the `voidline` cookie profile. Until this happens, every subsequent weekly-intel run
will hit the same wall.
