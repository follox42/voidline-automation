# Self-eval — 2026-W29

> Meta-review.

## LEARNINGS quality this week
- [x] All entries backed by ≥ 200 impressions? — N/A: this routine wrote no new performance-based
  claims. No Studio analytics were collected (camoufox-stealth MCP connector unreachable), so
  nothing was inferred from insufficient data. Correct failure mode is "no claim," not
  "low-confidence claim."
- [x] Any low-confidence claims flagged? — None made. `progress/snapshots/2026-W29.json`,
  `weekly_actions/2026-W29.md`, and `viewer_feedback/2026-W29.json` are all explicitly marked
  `BLOCKED`/`data_collection_status` rather than populated with placeholder or estimated numbers.

## Experiments status
- [x] All open experiments still measuring the right thing? — `experiment_tracker.py status`: 4
  open, still `4/3` over the cap first flagged in `self_eval/2026-W27.md` (2 weeks ago) — unresolved
  since. EXP-HOOK-001 / EXP-TITLE-001 / EXP-VOICE-001 (since 2026-06-30, 19d old) and
  PASSIVE-TIME-001 (since 2026-07-01, 18d old) are all now well past the 14-day minimum age, but
  **not eligible for a verdict on data grounds, not just the Studio outage**: `experiment_tracker.py
  check` reports all 4 "no result yet, agent must populate," and `min_videos: 3` cannot be satisfied
  regardless of Studio access — EXP-HOOK/TITLE/VOICE-001 have only 1 real tagged video
  (`mgdNSwtkrnw`); their two `pending_runs` (LONG-2/Ourang Medan, LONG-1/Hauser) were never actually
  published (script+assets only, blocked on the same ElevenLabs/cookie outage tracked since
  2026-07-02). PASSIVE-TIME-001 has its 1 required video (`Tlc-cKtAHuQ`, v4 Roanoke) but still needs
  Studio impressions data.
- [x] Verdicts written for any that hit 14d + 200 impressions? — None. Age threshold is met for all
  4; the blocker is data (Studio access for 3 of them, insufficient published-video count for the
  hook/title/voice trio).
- **Process finding, reconfirmed**: 4 open vs cap of 3, unchanged from `self_eval/2026-W27.md`'s
  recommendation to close one before opening a 4th — no new experiment was opened this week either,
  so the recommendation held, but the underlying over-cap condition itself is still not resolved
  (would require a verdict on one of the 4, which requires data this routine cannot fetch).
- **New finding**: even setting the Studio outage aside, EXP-HOOK-001/TITLE-001/VOICE-001 are
  structurally stalled — they need 3 tagged+published videos to reach `min_videos`, and the
  production pipeline has published exactly 0 new long-forms since 2026-07-01 (17 days). These
  three experiments cannot close on time even if Studio access is restored tomorrow; recommend
  flagging to whichever routine manages experiments that these are now bottlenecked on production
  output, not just analytics access.

## Failure modes recurrence
- [x] Any error hit 2+ times this week → escalate. **Yes — escalating, now a multi-week pattern.**
  The camoufox-stealth MCP connector was reconfirmed unreachable (`Error | Not connected`) twice
  this session (immediate retry, per the CLAUDE.md transient-failure protocol) and is documented as
  unreachable across **6 consecutive routine sessions** in `skills/voidline-manager/LEARNINGS.md`
  going back to 2026-07-18 (community-manager RUN62/RUN63, the 07-19 daily-short and daily-plan
  sessions, and now this weekly-intel run). This is the same root cause that blocked
  `BLOCKER_2026-07-05-WEEKLY-INTEL` two weeks ago, but the diagnosis has sharpened: RUN62 confirmed
  it is a connector-level outage (the MCP backend refuses any session), not merely the separately-
  dead voidline cookie (17 days unrefreshed since 2026-07-02) or the ElevenLabs quota exhaustion
  (resets 2026-07-30). Three independent owner-only blockers are now compounding: (1) camoufox-
  stealth connector down, (2) voidline cookie dead, (3) ElevenLabs quota exhausted. Together they
  have stalled effectively the entire production+publishing pipeline since 2026-07-02 — 0 long-forms
  and only 1 Short have gone `PUBLIC` in the last 17 days, and 5 more Shorts plus 2 long-forms sit
  produced-or-partial but unpublished.
- **New recurrence not previously logged**: the **2026-W28 weekly-intel run itself never executed**
  — no `progress/snapshots/2026-W28.json`, `weekly_actions/2026-W28.md`, or
  `viewer_feedback/2026-W28.json` were ever created (confirmed by their absence on disk, despite the
  2026-07-12 Idea Lock log entry referencing them as "BLOCKED" — it was reading state that was never
  scaffolded, not state that was collected-then-blocked). This is qualitatively worse than this
  week's or W27's outcome: W27 and W29 both ran the full 7-phase loop and produced honest
  blocked-markers; W28 appears to have been skipped entirely, which is itself a gap worth flagging —
  a routine that silently doesn't run is harder to notice than one that runs and reports "blocked."

## Calibration notes
This run's honest scope: **Phases 1, 2, 4, and 6 could not execute** — all require either YouTube
Studio analytics or the Studio comments inbox, both gated behind the unreachable camoufox-stealth
connector (confirmed via `camoufox-stealth_status` → `Error | Not connected`, retried once per
protocol, same result). No YouTube Data/Analytics API credentials are present in this environment
(`env | grep -iE 'youtube|google_api|yt_'` → none set), so there is no fallback data source; this
matches the conclusion already reached and `KNOWN_BAD`-documented in `self_eval/2026-W27.md`.

What *was* done honestly within the outage: scaffolded `progress/snapshots/2026-W29.json` with the
known list of last-30-days video/short IDs (compiled from `shorts/shorts_state.json`, not
fabricated) — only 4 items, versus 11 in the equivalent W27 snapshot, reflecting how much the
pipeline has stalled since; skipped the `weekly_curve.csv` append (still no real
`channel_aggregates` data to append — same precedent as W27, avoids poisoning the rolling average
with a blank row); ran the two sub-phases that don't need Studio access (experiment status/check —
surfaced the cap-over-limit and structural-stall findings above; `progress_curve.py eta` — correctly
reports "no data yet," since `weekly_curve.csv` still has zero rows two runs later); and wrote
`weekly_actions/2026-W29.md` / `viewer_feedback/2026-W29.json` with explicit blocked-markers so
downstream Production/Idea-Lock routines don't misread empty as "nothing to do."

**Confidence**: high that no fabricated data leaked into any output file this run. Low confidence
that next week's routine will fare differently — this is now week 3 of essentially the same
compounding blocker set (connector outage layered on top of the pre-existing dead cookie and
exhausted quota), and the W28 silent-skip shows the failure mode is getting worse, not better,
without owner intervention. The single highest-leverage fix remains unchanged across three
consecutive weekly-intel reports: an interactive owner session to re-login the voidline cookie and
restart/verify the camoufox-stealth MCP connector. ElevenLabs quota self-resolves 2026-07-30.
