# Self-eval — 2026-W27

> Meta-review.

## LEARNINGS quality this week
- [x] All entries backed by ≥ 200 impressions? — N/A this week: no new performance-based LEARNINGS
  entries were written by this routine, because no Studio analytics could be collected (see
  `BLOCKER_2026-07-05-WEEKLY-INTEL`). Nothing was claimed on insufficient data — the correct
  failure mode here is "no claim," not "low-confidence claim."
- [x] Any low-confidence claims flagged? — None made. `progress/snapshots/2026-W27.json`,
  `weekly_actions/2026-W27.md`, and `viewer_feedback/2026-W27.json` are all explicitly marked
  `BLOCKED`/`data_collection_status` rather than populated with placeholder or estimated numbers.

## Experiments status
- [x] All open experiments still measuring the right thing? — Reviewed via
  `experiment_tracker.py status`: 4 open (EXP-HOOK-001, EXP-TITLE-001, EXP-VOICE-001 since
  2026-06-30; PASSIVE-TIME-001 since 2026-07-01). Hypotheses still match current production
  direction (hook/title/voice variants, publish-time). No stale experiments to close.
- [x] Verdicts written for any that hit 14d + 200 impressions? — None eligible yet. Oldest
  (EXP-HOOK/TITLE/VOICE-001) are 5 days old, need 14; PASSIVE-TIME-001 is 4 days old. Ran
  `experiment_tracker.py check` — correctly reported all 4 as "too young," 0 closed. Note: even
  once 14 days pass, closing them needs `result_30d`/`result_7d` populated from real Studio
  metrics, which this week's outage did not provide and will not backfill retroactively.
- **Process finding (not from Studio data — read directly from `experiments/*.json`)**: the repo
  currently has **4 open experiments against a hard cap of 3** ("OPEN (4/3)" from
  `experiment_tracker.py status`). `PASSIVE-TIME-001` was added by directly writing the JSON file
  on 2026-07-01 (a "natural experiment" opportunistically declared from an accidental 00:00 UTC
  publish) rather than through `experiment_tracker.py open`, which would have refused a 4th slot.
  Recommend: do not open any new experiment until at least one of the current 4 closes (earliest
  eligibility 2026-07-14), and add a cap check to whatever wrote `PASSIVE-TIME-001.json` directly
  so this can't happen silently again.

## Failure modes recurrence
- [x] Any error hit 2+ times this week → escalate. **Yes — escalating.** The camoufox-stealth /
  mcphub Studio bridge has now failed **3 times in a single day** (2026-07-05): 06:30 UTC
  daily-short session, 08:10 UTC daily-plan session (commit `ce615ab`), and this 10:xx UTC
  weekly-intel session. All three independently report the same signature: mcphub returns
  HTTP 530 / the direct camoufox-stealth fallback returns 502, and no `camoufox-stealth`/`mcphub`
  MCP tools are registered via ToolSearch. This is qualitatively different from the
  previously-logged blockers (classifier policy denials on write actions, or cross-host filesystem
  isolation on upload) — this looks like the mcphub service itself being down/unreachable at the
  infrastructure level, not a per-action policy block. Given the entire skill fleet (weekly-intel,
  daily-short uploads, comment mining, community-tab posting) depends on this single bridge, a
  same-day 3x outage should be treated as a service-health incident, not routine noise.

## Calibration notes
This run's honest scope: **Phases 1, 2, 4, and 6 of the 7-phase weekly-intel plan could not
execute** — all of them require either YouTube Studio analytics or the Studio comments inbox,
both gated behind the down bridge. Confirmed no viable substitute exists: no YouTube Data/
Analytics API credentials are present in this environment (checked `YOUTUBE_API_KEY`,
`YOUTUBE_OAUTH_TOKEN`, `GOOGLE_API_KEY`, `YT_ANALYTICS_TOKEN`, `YOUTUBE_CLIENT_SECRET` — all
unset), and public-page fallbacks don't substitute either (WebFetch on the public watch page hit
YouTube's logged-out bot-check redirect; the channel `/about` page returned only the JS-shell nav
chrome with no rendered stats — consistent with the already-`KNOWN_BAD` "anonymous curl to
youtube.com is unreliable" finding from 2026-06-13).

What *was* done honestly within the outage: scaffolded `progress/snapshots/2026-W27.json` with the
known list of last-30-days video/short IDs (compiled from `shorts/shorts_state.json` and
LEARNINGS, not fabricated), left every metrics field `null`, skipped the `weekly_curve.csv` append
entirely (an all-blank/zero row this early — the very first row ever — would poison the 4-week
average the ETA calculation depends on for every future run), ran the two sub-phases that don't
need Studio access at all (experiment status/check, progress-curve ETA — correctly reported "no
data yet," this is the channel's first weekly-intel run), and wrote `weekly_actions/2026-W27.md` /
`viewer_feedback/2026-W27.json` with explicit blocked-markers so downstream Production/Idea-Lock
routines don't misread empty as "nothing to do."

**Confidence**: high that no fabricated data leaked into any output file this run. Low confidence
that next week's routine will fare differently unless the mcphub outage itself is diagnosed and
fixed outside of a routine session — this is now a 3x-same-day pattern, not a one-off flake.
